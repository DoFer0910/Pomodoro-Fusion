## [v0.3.8] - 2026-02-27

### 概要
通常モード復帰後にウィンドウの端をドラッグしてサイズ変更しようとした際、ウィンドウが意図せず縮小してしまうバグを修正しました。

### 変更点
- **修正 (fix):** コンパクトモードから通常モードへの復帰時、OS側のドラッグ時のサイズ情報が正しく更新されるように、`setResizable(true)` を `setBounds()` の直前に呼び出すよう `electron/main.js` の処理順序を修正しました。

### 注意事項
運用への影響はありません。
